{
  "name": "corners",
  "version": "1.0.0",
  "description": "Play Corners",
  "scripts": {
    "start": "npm run serve",
    "serve": "node dist/server.js",
    "build": "npm run copy-static-files && npm run build-ts",
    "build-ts": "tsc",
    "copy-static-files": "ts-node copyStaticFiles.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael B",
  "license": "MIT",
  "dependencies": {
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/cpx": "^1.5.0",
    "@types/express": "^4.17.1",
    "@types/node": "^12.7.5",
    "cpx": "^1.5.0",
    "ts-node": "^8.4.1",
    "typescript": "^3.6.3"
  }
}
